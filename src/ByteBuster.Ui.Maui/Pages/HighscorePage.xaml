<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ByteBuster.Ui.Maui.Pages.HighscorePage"
             xmlns:viewmodel="clr-namespace:ByteBuster.Ui.Maui.ViewModels"
             x:DataType="viewmodel:HighscoreViewModel"
             Title="{Binding Title}">
    <Grid>
        <Image
            Source="stacked_waves.png"
            Aspect="AspectFill"
            VerticalOptions="Fill"
            HorizontalOptions="Fill" />
        <VerticalStackLayout
            VerticalOptions="Fill"
            HorizontalOptions="Fill">

            <Label Text="Label inside of StackLayout with an image in the background" />

        </VerticalStackLayout>

    </Grid>
</ContentPage>